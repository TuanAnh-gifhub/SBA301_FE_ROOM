name: CICD_test

on:
  push:
    branches: ["feature/be/notification"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Backend
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          push: ${{ github.event_name == 'push' }}
          tags: ${{ secrets.DOCKER_USERNAME }}/rent-room-backend:latest

    # - name: Build & Push Frontend
    #   uses: docker/build-push-action@v4
    #   with:
    #     context: ./frontend
    #     push: ${{ github.event_name == 'push' }}
    #     tags: ${{ secrets.DOCKER_USERNAME }}/rent-room-frontend:latest
    #     build-args: |
    #       VITE_API_URL=${{ secrets.VITE_API_URL }}
    #       VITE_GOOGLE_ID=${{ secrets.VITE_GOOGLE_ID }}

  # deploy:
  #   # Chỉ chạy khi job build-and-push thành công
  #   needs: build-and-push

  #   runs-on: ubuntu-latest

  #   steps:
  #     # SSH vào EC2 để deploy
  #     - name: SSH to EC2 and Deploy
  #       uses: appleboy/ssh-action@v0.1.10
  #       with:
  #         # Thông tin server lấy từ GitHub Secrets
  #         host: ${{ secrets.EC2_HOST }} # Public IP hoặc domain EC2
  #         username: ${{ secrets.EC2_USER }} # Ví dụ: ubuntu
  #         key: ${{ secrets.EC2_SSH_KEY }} # Private key SSH
